<!-- Short PR description — what changes and why. -->

## What

## Why

## Verification

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] If a public CLI flag or library type changed, `README.md` and
      `CHANGELOG.md` were updated
- [ ] No secrets / API keys in diff (run a quick `git diff | grep -iE
      'sk-|api_key|secret'` before pushing)

## Notes for reviewer

<!-- Anything provider-specific, edge cases worth eyeballing, links to
     issues, etc. -->
